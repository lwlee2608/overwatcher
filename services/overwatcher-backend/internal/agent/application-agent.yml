log:
  level: info
agent:
  name: ""                   # optional; descriptive label only (override with AGENT_NAME)
  coordinator_url: "http://localhost:8080"
  token: ""                  # required; override with AGENT_TOKEN
  poll_timeout: 30s          # client-side timeout; must exceed coordinator's 25s long-poll
